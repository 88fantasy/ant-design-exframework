import { Checkbox, DatePicker, Input } from 'antd';
import Hov from '../../Hov';

const { RangePicker } = DatePicker;
const { Group } = Checkbox;

export default {
  Input,
  Group,
  RangePicker,
  Hov,
};
